<UserControl x:Class="QuanLyDuLich.GUI.View.TourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh công cụ -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtSearch" Width="200" VerticalContentAlignment="Center" Margin="0,0,10,0" Padding="5"/>
                <Button Content="🔍 Tìm kiếm" Click="BtnSearch_Click" Padding="10,5" Background="#3498DB" Foreground="White" BorderThickness="0"/>
                <Button Content="🔄" Click="BtnRefresh_Click" Margin="5,0" Padding="10,5"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="+ Thêm Mới" Click="BtnAdd_Click" Background="#2ECC71" Foreground="White" Padding="15,5" BorderThickness="0"/>
                <Button Content="✏️ Sửa" Click="BtnEdit_Click" Margin="5,0" Background="#F1C40F" Foreground="White" Padding="15,5" BorderThickness="0"/>
                <Button Content="🗑️ Xóa" Click="BtnDelete_Click" Background="#E74C3C" Foreground="White" Padding="15,5" BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <!-- Bảng dữ liệu -->
        <DataGrid Grid.Row="1" x:Name="dgTours" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Tour" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Tên Tour" Binding="{Binding TourName}" Width="*"/>
                <DataGridTextColumn Header="Giá Gốc" Binding="{Binding BasePrice, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                <DataGridTextColumn Header="Ngày" Binding="{Binding Dura}" Width="60"/>
                <DataGridTextColumn Header="Loại Tour" Binding="{Binding TourType}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>