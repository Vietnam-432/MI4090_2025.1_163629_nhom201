<UserControl x:Class="QuanLyDuLich.GUI.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="QUẢN LÝ NGƯỜI DÙNG HỆ THỐNG" 
                   FontSize="24" FontWeight="Bold" Foreground="#8E44AD" 
                   Margin="20,20,0,10"/>

        <!-- Thanh công cụ -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,0,20,10">
            <Button x:Name="btnAdd" Content="➕ Thêm User" Click="BtnAdd_Click"
                    Background="#3498DB" Foreground="White" Padding="15,8" Margin="0,0,10,0" BorderThickness="0"/>
            <Button x:Name="btnEdit" Content="✏️ Đổi Mật Khẩu/Sửa" Click="BtnEdit_Click"
                    Background="#F39C12" Foreground="White" Padding="15,8" Margin="0,0,10,0" BorderThickness="0"/>
            <Button x:Name="btnDelete" Content="🗑️ Xóa" Click="BtnDelete_Click"
                    Background="#E74C3C" Foreground="White" Padding="15,8" BorderThickness="0"/>
        </StackPanel>

        <!-- Danh sách dữ liệu -->
        <DataGrid x:Name="dgData" Grid.Row="2" Margin="20,0,20,20"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" Background="WhiteSmoke" RowHeaderWidth="0">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="Background" Value="#ECF0F1"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên Đăng Nhập" Binding="{Binding UserName}" Width="200"/>
                <!-- Không hiển thị mật khẩu trực tiếp -->
                <DataGridTextColumn Header="Mật Khẩu" Binding="{Binding Password}" Width="150" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Mã Vai Trò" Binding="{Binding RoleId}" Width="100"/>

                <!-- Cột hiển thị Vai trò dạng chữ (dùng Converter hoặc logic đơn giản) -->
                <DataGridTemplateColumn Header="Vai Trò" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding RoleId}" VerticalAlignment="Center"/>
                            <!-- Lưu ý: Bạn có thể viết ValueConverter để chuyển 0 -> Admin, 1 -> Kế toán -->
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>